% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adverse-events.R
\name{aeSummary}
\alias{aeSummary}
\title{Summarize Adverse Events by Trial Arm}
\usage{
aeSummary(events, allocation, arms = NULL)
}
\arguments{
\item{events}{List of \code{\link[=adverseEvent]{adverseEvent()}} rows or a data frame using the same
schema.}

\item{allocation}{Data frame with unique \code{participant_id} and \code{arm}.}

\item{arms}{Optional complete arm order, including zero-event arms.}
}
\value{
An \code{ae_summary} with arm, severity, and term tables.
}
\description{
Reports event counts separately from participant incidence, and counts SAE
status from the explicit \code{serious} field rather than a CTCAE grade cutoff.
}
\examples{
allocation <- data.frame(
  participant_id = c("p1", "p2"),
  arm = c("active", "control")
)
events <- list(adverseEvent(
  "p1", "Nausea", as.Date("2026-01-02"),
  ctcae_grade = 1
))
aeSummary(events, allocation, arms = c("active", "control"))
}
