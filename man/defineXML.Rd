% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdisc.R
\name{defineXML}
\alias{defineXML}
\title{Create a Deterministic define.xml Metadata Stub}
\usage{
defineXML(
  x,
  file = NULL,
  study_name = NULL,
  protocol_name = NULL,
  standard_version = NULL
)
}
\arguments{
\item{x}{An \code{sdtm_export} or \code{adam_export}.}

\item{file}{Optional output path. Writing uses a temporary file followed by
an atomic rename.}

\item{study_name, protocol_name}{Optional display metadata.}

\item{standard_version}{Optional standard-version label.}
}
\value{
Invisibly, the XML string when \code{file} is \code{NULL}; otherwise the
normalized written path.
}
\description{
Serializes metadata for the exact datasets in an SDTM-shaped or ADaM-shaped
export. The result is a deterministic ODM metadata stub, not a
submission-ready define.xml document.
}
\examples{
trial <- Trial("T1", c("A", "B"))
participants <- data.frame(id = c("p1", "p2"), arm = c("A", "B"))
xml <- defineXML(toSDTM(trial, participants))
grepl("ItemGroupDef", xml, fixed = TRUE)
}
