% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-sets.R
\name{intentionToTreat}
\alias{intentionToTreat}
\title{Intention-to-Treat Analysis Set}
\usage{
intentionToTreat(
  trial,
  participants,
  id_col = "id",
  arm_col = "arm",
  randomized_col = "randomized"
)
}
\arguments{
\item{trial}{A \link{Trial} object.}

\item{participants}{One row per source participant.}

\item{id_col, arm_col, randomized_col}{Column names in \code{participants}.}
}
\value{
An \code{analysis_set} with included rows, member IDs, and explicit
exclusions.
}
\description{
Includes every randomized participant in the originally assigned arm.
Missing outcomes, adherence, and post-randomization events do not alter
membership.
}
\examples{
trial <- Trial("T1", c("active", "control"))
participants <- data.frame(
  id = c("p1", "p2", "screen"),
  arm = c("active", "control", NA),
  randomized = c(TRUE, TRUE, FALSE)
)
intentionToTreat(trial, participants)
}
