% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis-sets.R
\name{perProtocol}
\alias{perProtocol}
\title{Per-Protocol Analysis Set}
\usage{
perProtocol(
  trial,
  participants,
  deviations = NULL,
  id_col = "id",
  arm_col = "arm",
  randomized_col = "randomized",
  deviation_id_col = "participant_id",
  major_col = "major",
  code_col = "code",
  adherent_col = NULL,
  completed_col = NULL
)
}
\arguments{
\item{trial}{A \link{Trial} object.}

\item{participants}{One row per source participant.}

\item{deviations}{Optional protocol-deviation data frame.}

\item{id_col, arm_col, randomized_col}{Column names in \code{participants}.}

\item{deviation_id_col, major_col, code_col}{Columns in \code{deviations}.}

\item{adherent_col, completed_col}{Optional logical columns in
\code{participants}.}
}
\value{
A \code{PP} \code{analysis_set}.
}
\description{
Starts from ITT and excludes participants with a major protocol deviation or
caller-selected nonadherence/incompletion flags. Multiple exclusion reasons
are retained in a deterministic order.
}
\examples{
trial <- Trial("T1", c("active", "control"))
participants <- data.frame(
  id = c("p1", "p2", "screen"),
  arm = c("active", "control", NA),
  randomized = c(TRUE, TRUE, FALSE),
  adherent = c(TRUE, FALSE, NA)
)
perProtocol(trial, participants, adherent_col = "adherent")
}
