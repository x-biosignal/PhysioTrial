% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power.R
\name{sampleSizeContinuous}
\alias{sampleSizeContinuous}
\title{Sample size for a two-arm continuous endpoint}
\usage{
sampleSizeContinuous(
  delta,
  sd,
  power = 0.8,
  sig_level = 0.05,
  ratio = 1,
  sides = 2L,
  dropout = 0,
  cluster_size = NULL,
  icc = NULL
)
}
\arguments{
\item{delta}{Target difference in means (treatment - control).}

\item{sd}{Common (residual) standard deviation of the endpoint.}

\item{power}{Target power (default 0.8).}

\item{sig_level}{Two-sided (or one-sided, see \code{sides}) alpha (default 0.05).}

\item{ratio}{Allocation ratio n2/n1 (treatment/control; default 1).}

\item{sides}{1 or 2 (default 2).}

\item{dropout}{Expected proportion lost to follow-up; inflates enrolment
(default 0).}

\item{cluster_size}{Average subjects per cluster for a cluster-randomized
design (\code{NULL} = individually randomized).}

\item{icc}{Intra-cluster correlation (required with \code{cluster_size}).}
}
\value{
A \code{trial_power} object (\code{n1}, \code{n2}, \code{n_total},
\code{n_enrolled}, design effect, clusters, ...).
}
\description{
Two-sample comparison of means. Delegates the balanced case's core to the
noncentral-t (matching \code{stats::power.t.test}) and adds unequal
allocation, cluster design effect, and dropout inflation.
}
\examples{
sampleSizeContinuous(delta = 5, sd = 10)          # d = 0.5 -> 64/arm
sampleSizeContinuous(delta = 5, sd = 10, dropout = 0.2)
}
\seealso{
\code{\link[=sampleSizeANCOVA]{sampleSizeANCOVA()}}, \code{\link[=sampleSizeBinary]{sampleSizeBinary()}}, \code{\link[=powerContinuous]{powerContinuous()}}
}
