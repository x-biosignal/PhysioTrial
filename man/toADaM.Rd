% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdisc.R
\name{toADaM}
\alias{toADaM}
\title{Export Trial Analysis Sets to ADaM-Shaped Data}
\usage{
toADaM(
  set,
  subject_data,
  bds = NULL,
  study_id = set$trial_id,
  id_col = "id",
  treatment_col = "arm",
  population_flags = NULL
)
}
\arguments{
\item{set}{The authoritative primary \code{analysis_set}.}

\item{subject_data}{One row per source participant.}

\item{bds}{Optional generic BDS source data frame. A named list may contain
an \code{ADBDS} source plus already constructed analysis datasets such as
\code{ADQS}.}

\item{study_id}{Non-empty study identifier.}

\item{id_col, treatment_col}{Subject ID and originally assigned treatment
columns.}

\item{population_flags}{Optional named list of other \code{analysis_set}
objects from the same source population.}
}
\value{
An \code{adam_export} containing datasets, manifest, and population
metadata.
}
\description{
Builds an ADSL population table and a generic BDS analysis table from
explicit analysis sets. This structural export is not a claim of formal
ADaM conformance.
}
\examples{
trial <- Trial("T1", c("active", "control"))
subjects <- data.frame(
  id = c("p1", "p2", "screen"),
  arm = c("active", "control", NA),
  randomized = c(TRUE, TRUE, FALSE)
)
itt <- intentionToTreat(trial, subjects)
export <- toADaM(itt, subjects)
export$datasets$ADSL
}
