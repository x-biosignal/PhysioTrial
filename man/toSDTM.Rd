% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdisc.R
\name{toSDTM}
\alias{toSDTM}
\title{Export Trial Data to CDISC SDTM-Shaped Domains}
\usage{
toSDTM(
  trial,
  participants,
  adverse_events = NULL,
  findings = list(),
  study_id = trial@id,
  id_col = "id",
  arm_col = "arm",
  sex_col = NULL,
  birth_date_col = NULL,
  reference_start_col = NULL,
  reference_end_col = NULL,
  country_col = NULL,
  site_col = NULL,
  extra_domains = list()
)
}
\arguments{
\item{trial}{A \link{Trial} object.}

\item{participants}{One row per screened or enrolled participant.}

\item{adverse_events}{Optional adverse events accepted by \code{\link[=aeSummary]{aeSummary()}}.}

\item{findings}{Named list containing normalized \code{VS}, \code{EG}, or \code{XP}
findings data frames.}

\item{study_id}{Non-empty study identifier.}

\item{id_col, arm_col}{Participant ID and assigned-arm columns.}

\item{sex_col, birth_date_col, reference_start_col, reference_end_col}{Optional
participant columns.}

\item{country_col, site_col}{Optional country and site columns.}

\item{extra_domains}{Named list of already constructed two-letter domains,
such as output from \code{PhysioClinical::toCDISC_QS()}.}
}
\value{
An \code{sdtm_export} containing datasets, manifest, and mapping metadata.
}
\description{
Builds deterministic, linked trial domains with explicit sponsor mappings.
The output is structurally SDTM-shaped; it is not a claim of CDISC,
regulator, or third-party validator conformance.
}
\examples{
trial <- Trial("T1", c("active", "control"))
participants <- data.frame(
  id = c("p1", "p2"),
  arm = c("active", "control")
)
export <- toSDTM(trial, participants)
export$datasets$DM
}
